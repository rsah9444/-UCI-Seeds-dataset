# -UCI-Seeds-dataset
 build and evaluate a feed-forward artificial neural network (ANN) for a multiclass classification problem using the UCI Seeds dataset. The dataset contains 210 samples, 7 numeric features, and 3 classes. 
